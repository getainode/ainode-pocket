__pycache__/
*.py[cod]
.DS_Store
*.swp
.venv/
# Never commit device credentials or local state. Keys live in
# ~/.tiinyapps/device.json or ~/.ainode-pocket/, never in the repo.
devices.json
device.json
*.key
bench-results/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
